// Parallel route slot rendered alongside `children` in the root layout for
// every route that doesn't match one of the intercepting routes below it
// (app/@modal/(.)movie|series|book/[id]) - i.e. nothing to show.
export default function Default() {
  return null
}
